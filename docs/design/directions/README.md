# Superseded — v1 exploration

These three directions (huashu/editorial, impeccable, frontend) were explored
before choosing "The Ledger" as the dashboard's first visual identity. The
Ledger itself was later fully replaced — see
[`../design-system.md`](../design-system.md).

Kept for history only. Do not implement against these files.
