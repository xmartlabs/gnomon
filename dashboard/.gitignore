node_modules/
.next/
*.db
.env
*.tsbuildinfo
test-results/
playwright-report/

# Local dev data volume (DATA_DIR=$PWD/.data). Holds the jwt signing secret and
# the WAL sidecars, which carry the same uploaded data as the .db itself.
.data/
*.db-wal
*.db-shm
jwt-secret
